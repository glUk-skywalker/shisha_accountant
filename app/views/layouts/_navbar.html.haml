#navbar
  = link_to(image_tag('logout-icon.png'), signout_path, class: 'navitem right logout', title: 'Logout') if current_user
  .navitem.right= @user.first_name
  = image_tag current_user.photo_url, alt: 'ava', class: 'navitem right ava'
